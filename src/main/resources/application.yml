spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seed_planner?&rewriteBatchedStatements=true
    username: admin
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
      # entity table name custom
      naming:
        physical-strategy : com.seed_planner.schedule_center.common.CustomNamingStrategy
